# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/) 风格和 SemVer 版本号。

## [Unreleased]

### Added

- 支付模块接线：注册管理端支付配置/渠道/订单 API、用户充值下单 API 和 Stripe webhook 回调路由，全部由 `E2M_ENABLE_PAYMENTS` 运行开关控制（默认关闭，关闭时返回 `404 feature_disabled`）。
- 新增支付订单到期清扫后台任务：超时未支付订单在关闭上游会话后转为 EXPIRED；清扫前主动查询上游，已支付但回调丢失的订单走与 webhook 相同的恰好一次入账路径补单（`E2M_PAYMENT_EXPIRY_INTERVAL` 可调，默认 60s）。
- 控制台（`VITE_E2M_ENABLE_PAYMENTS` 构建开关下）：新增用户"余额充值"页（金额与支付方式选择 → 创建订单 → 跳转服务商托管支付页）与支付结果/取消回跳页；挂载既有"收款订单（试验）"管理页与系统设置支付 tab 到路由和菜单。
- 新增"模型市场"（PM-07）：客户可见的分组 × 模型 × 到手价报价页（`GET /api/v1/platform/model-market` 返回每组每模型的当前最优售价，不暴露上游身份/成本/容量），控制台 `/model-market` 页面随平台分发常开；客户总览新增"平台消费"卡片（钱包余额、今日结算消费与快捷入口）；管理端分组表单新增"售价倍率"字段。
- 新增基准价目表定价（PM-06）：内置 LiteLLM 格式基准价快照（可经 `E2M_PRICE_TABLE_PATH` 挂载覆盖），按 `E2M_USD_TO_CNY_RATE` 折算 CNY；平台分组新增 `rate_multiplier` 售价倍率（0.0001–100，标签承载）；创建上游不填价格时自动按"基准价 × 汇率 × 分组倍率"物化定价（显式价格永远优先，未收录模型或多模型异价时 fail-closed 要求显式定价）；新增管理端定价预览端点 `GET /api/v1/platform/pricing/preview`。汇率未配置时基准价定价整体禁用。
- 新增兑换码域（`E2M_ENABLE_PAYMENTS` 开关下）：余额/邀请两种类型、批量生成（≤1000，明文仅生成响应展示一次，库中只存 SHA-256 哈希与前缀）、用户兑换端点（失败限速、恰好一次入账、余额与钱包流水联动）、管理端列表/停用/删除，以及面向外部发卡系统的 `create-and-redeem` 幂等 API（同 Idempotency-Key 重放返回原结果、异载荷 409、崩溃恢复续兑）。控制台新增用户"兑换码"页与管理端"兑换码管理"页。
- 新增 EasyPay（易支付聚合协议）收款渠道：MD5 签名的托管支付页下单、GET/POST notify 回调验签（应答纯文本 success）、商户查单 API；充值下单按渠道选择 Stripe 或 EasyPay（支付宝/微信类型，仅 CNY），到期清扫器对 EasyPay 订单同样先查单补单再本地过期。

### Fixed

- 修复充值日限额只统计当日前 100 笔已完成订单的缺陷，改为分页累加并在异常规模时保守拒绝。
- 修复文件 Vault 的可用性缺陷：`Resolve` 的跨进程刷新重载失败时（如路径读错误），不再拒绝服务本进程内存中一致持有的凭证；仅当内存中也没有该引用时才返回读错误。该缺陷因 Linux/Windows 错误码映射差异只在 Linux 暴露，曾导致 `file_test.go` 两个失败注入测试稳定失败。

### Changed

- 支付相关路径（含 webhook 与充值下单）的功能门禁不再依赖已退役的 `E2M_ENABLE_HYBRID_SUPPLY` 实验开关，仅由 `E2M_ENABLE_PAYMENTS` 控制。

## [0.3.0] - 2026-08-01

### Added

- 控制台补齐平台分组和 OpenAI-compatible 上游的编辑、启停、安全退役操作。
- 上游支持多模型、优先级、权重、并发与容量配置，并可测试连接和拉取模型目录。
- 新增分组退役、上游安全下线和 Vault 写入失败隔离的服务端保护与回归测试；Core 后台会自动续跑持久化的分组退役任务。

### Changed

- 按 SemVer 将工作区及控制台版本提升至 `0.3.0`。

## [0.2.0] - 2026-08-01

### Added

- 完整的平台分发能力：支持创建分组、接入上游账号、加密保存凭证，以及模型、价格和容量校验。
- 新增 OpenAI 兼容的分组路由、余额/用量记录、错误转移和管理 API。
- 控制台新增分组创建、上游接入、密钥、供应和运行状态管理界面。
- 补齐 Connector 生命周期、健康检查、上游情报、路由、计费、通知、支付和运营闭环。
- 增加数据库迁移、部署模板、工程化 CI、提交检查和安全扫描工作流。

### Changed

- 按 SemVer 将工作区及控制台版本提升至 `0.2.0`。
