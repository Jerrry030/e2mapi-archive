# 页面与角色首轮改造补充（2026-07-21）

以下内容补充 `page-role-rework-2026-07-21.md`：

- client 首页已经改用 owner onboarding 与 owner pool health 的业务事实，展示服务已就绪实例、可调度线路、开放事故和需要用户处理的实例；不再把旧 `Instance.status` 当成托管服务健康结论。
- admin 首页已经接入运维中心汇总，展示开放事故、接入待重试和观测覆盖率，并提供运维中心入口。
- 不完整的账单、支付和供给模块不仅从菜单隐藏；对应直达路由也受同一构建开关保护，默认访问会回到首页。
