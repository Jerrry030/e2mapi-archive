# E2M 事实基线勘误（2026-07-20）

本文件优先于同日的 `fact-baseline-2026-07-20.md`，只修正“上游账号”验收入口这一处表述：

- `实例管理 → 上游账号` 不是平台托管交付的验收页。
- 普通 `client` 查询账号时会过滤由 RoutePlan 管理的平台账号；该页的手动启停和一键切换也只面向用户自有账号。
- 平台托管交付应在 `接入与资源 → 我的 API Key` 验证 Key 分配与本地 Binding，在 `监控与处置 → 账号健康` 验证已发布量、可调度量、SLA 和自动切换结果。
