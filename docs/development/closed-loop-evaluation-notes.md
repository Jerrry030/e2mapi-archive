# 闭环环节评估记录

更新时间：2026-07-11

评估方式：按 `current-closed-loop-and-data-source.md` 中的闭环线路逐环节验收。用户负责体验和判断问题，Codex 负责记录意见、归类影响、给出修复方案，并在每个环节结束后提示下一环节的目标、已实现能力和建议确认点。

> 历史说明：下表保留每次验收发生时的事实快照。因此第 5、5.1、
> 5.2 行中的 Connector protocol v1、四项动作和 migration `20` 是
> 2026-07-10/11 的历史基线，不代表当前实现。当前已升级为 protocol
> v2，支持 typed create/update/delete、本地 write-only binding、30 分钟
> 延迟删除、Sub2API 主动探测和分阶段恢复；现状以
> `current-closed-loop-and-data-source.md`、`connector-lifecycle-v2.md` 和
> `managed-upstream.md` 为准。

## 当前节奏

1. 注册 / 登录
2. 首屏引导
3. 凭证写入
4. 接入实例
5. 安装连接器
6. 配置通知
7. 发布验证
8. 运维处置
9. 审批
10. 审计 / 账单
11. 供给能力闭环
12. 账号能力切换

## 评估记录

| 序号 | 环节 | 用户意见 / 问题 | 影响判断 | 修复方案 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1 | 注册 / 登录 | 1. “姓名 / 昵称”的占位符使用“站长姓名”，容易把注册人限定为站长本人。2. “站点 / 团队名称”在个体账号模型下多余。3. 用户纠正产品定位：E2M 面向个体用户，不存在组织团队、多岗位协作或多级分销；单个账号与单个邮箱绑定。4. 用户不认可继续复用 `tenant_id`，要求项目初期按最优模型直接精简。 | 第 1 点为明确体验问题。第 2/3/4 点说明此前把工作区解释为多人组织容器是错误定位。正确判断：邮箱是用户感知上的登录主体；资源隔离应直接围绕 `users.id` / `user_id`，不再保留独立租户抽象。 | 已执行：注册页标题为“注册”，副标题为“创建账号并进入控制台”；字段只保留登录邮箱、用户名 / 昵称、密码；后端删除 `tenant_name` 注册参数；运行时资源归属从 `tenant_id` 切换为 `user_id`；删除租户 API、租户页面和主路径租户模型；控制台用户可见表达改为个体账号、托管能力、供给能力。 | 已执行，待用户验收 |
| 1.1 | 注册 / 登录 | 用户希望账号 ID 更简洁，采用简单自增数字编号。 | 合理。项目初期不应保留随机字符串用户 ID、双 ID 或展示层伪编号；应直接参考 sub2api 的 `users.id BIGSERIAL` 模式，把数字 ID 作为资源隔离主键。 | 已执行：`users.id` 改为数字自增；业务资源字段统一为数字 `user_id` / `supplier_user_id`；控制台用户模型和筛选参数改为 number；用户管理列表展示为“编号 #数字”；迁移层不再保留旧 `tenant_id` / 字符串用户 ID 兼容，新库直接初始化为数字账号 ID。 | 已执行，待用户验收 |
| 4 | 接入实例 | 1. “所属用户”字段、外部用户/工作区下拉是冗余；当前账号操作的资源天然归属当前账号。2. “项目 ID、端点引用、凭证引用”过于内部化。3. 连接器可选项无数据时显示 no data，打断最小接入流程。4. 用户要求全项目按“能简则简、最小可用”处理。 | 判断合理。接入实例的 MVP 应只登记实例名称和网关类型；`project_id`、`endpoint_ref` 无当前用户价值，网关地址与管理密钥都属于连接器本地边界，不应传给 Core。普通用户也不应在其他页面看到归属筛选。 | 已执行：接入实例页表单仅保留实例名称、网关类型；后端创建/更新实例严格拒收 `gateway_url`、`credential_ref`、`admin_key`、`api_key` 等本地字段；列表移除 Agent/项目列；普通用户不再渲染 UserSelect；凭证、通知、连接器、审计、账单、供给等页面普通路径隐藏归属字段，平台管理员保留“账号”筛选。 | 已执行，待用户验收 |
| 4.1 | 全局账号隔离模型 | 用户要求不局限于接入实例页，审视全局是否仍误理解为“单账号下存在多空间”。 | 判断合理。当前运行时主路径已切为 `users.id` / `user_id` 直接归属，未发现普通账号可见的工作区/租户切换入口；项目仍处雏形期，不需要维护旧库升级兼容，迁移脚本也应直接落到最终账号模型。 | 已执行：控制台角色展示从“接入方/供应方”收敛为“托管能力/供给能力”和“托管模式/供给模式”；供给台账列名改为“托管账号/供给账号”；安全策略、路线图、边界文档、真实网关交接文档和现行架构文档改为账号隔离表达；评估文档记录“账号即资源归属边界”；删除旧租户/字符串用户 ID 兼容迁移，新库直接初始化。 | 已执行，待用户验收 |
| 4.2 | 接入实例 / 安装连接器 | 1. 实例信息填错后找不到编辑入口。2. 生成连接器要填写网关类型、认证方式、Connector ID、镜像、容器名等信息，流程过重。3. 用户明确建议创建实例时自动生成连接器便捷选项，但安装与否由用户决定。 | 判断合理。实例是用户频繁修正的基础资源，必须有编辑入口。连接器在用户心智上应是“为这个实例复制安装配置”，不应要求理解独立连接器参数。创建实例时自动准备连接器安装指南可以降低后续操作成本，只要不强制安装、不自动产生在线状态，就不会剥夺用户选择权。 | 已执行：托管实例列表增加“编辑”和“安装连接器”；创建实例成功后自动返回并弹出该实例的连接器安装指南；指南只包含 Core URL、实例/连接器绑定身份、一次性 enrollment 文件和本地 UI 入口，不包含网关地址、认证方式或凭证；连接器首次上线后自动绑定到实例；连接器页的生成流程收敛为只选择实例，不再让用户填写 Connector ID、认证方式、镜像、容器名等内部字段。 | 已执行，待用户验收 |
| 4.3 | 网关密钥边界 | 用户指出：创建实例时仍要求输入网关 API key，与此前“用户自行在连接器中设置，不能传给主服务器”的构想冲突。 | 判断完全合理。这是产品安全边界问题，不只是表单体验。Core 保存实例元信息和已绑定用户/实例的一次性 enrollment；网关 admin key、new-api token、CPA bearer token 必须留在对应连接器的本地持久卷。 | 已执行：实例产品面移除网关地址和凭证；认证管理请求只经每实例一个 Connector；安装配置使用 enrollment token file，不声明 owner；网关地址、认证方式和凭证通过本地 UI 保存；真实网关 bootstrap 初始化独立 Connector 卷，不向 Core 或容器环境注入网关凭证。 | 已执行，待用户验收 |
| 5 | Core / Connector 职责分离 | 用户确认由 Connector 暴露本地前端，网关地址、认证和后续本地配置均由用户在 Connector 完成，并要求按最终模型重构。 | 方向正确。Core 应只做身份、实例、授权、审批、审计和 typed task；每个 Instance 只绑定一个 Connector。任何全局机器 token、Core 云直连或通用网关凭证入口都会破坏实例边界。 | 已执行：Connector protocol v1 收敛为 health/list/schedulable/switch 四项已支持动作；本地 UI 使用 `#token` 和私有配置文件；删除 Agent heartbeat、云直连、全局健康 ingest token、Core Vault 明文 seed 和调用方自定义 secret ref；未实现本地 binding 管理前关闭账号 provision/update/delete 能力声明及用户入口；补充任务 lease nonce，防止旧租约进程提交过期结果。 | 已执行，待用户验收 |
| 5.1 | Connector 本地配置闭环 | 用户要求把连接器作为独立基础版本下发，网关地址和管理凭证由用户在连接器本地前端配置，Core 不得接收。 | 已验证该边界可落地。安装脚本不能预写配置文件或通过环境变量注入凭证，应与真实用户一样调用 loopback-only 本地 API；同一业务动作在旧任务终态后必须允许重试。 | 已执行并实测：bootstrap 改为读取 `local-ui.token`，经 `/api/local/connector/test` 与 `/config` 保存；Connector v1 数据库和代码均只接受四种任务；任务幂等唯一性仅覆盖活跃任务，终态后可重新发起。真实 sub2api 返回 2 个账号，账号 `2` 的 `schedulable` 成功 `true -> false -> true`。Core 数据库、日志和环境均未出现 `http://sub2api:8080` 或管理密钥，Connector 本地 `gateway-config.json` 权限为 `0600`。 | 已执行，待用户验收 |
| 5.2 | 最终结构 / 真实运行验收 | 重构后必须确认旧运行库不因迁移版本号相同而继续保留旧表、旧列，同时不能破坏真实网关数据。 | 项目雏形期应以全新 schema 为准；只允许重置 Core 控制面与 Connector 私有测试状态，sub2api、new-api、CPA 的真实数据卷必须保持原样。 | 已执行：空库及真实 Core 库均确认 migration `20`、`dirty=false`，旧租户/静态能力/Agent 心跳表和 Instance/Connector 冗余列均不存在；task DB CHECK 仅含四项 v1 action。真实 sub2api 返回账号 `2,1`，账号 `2` 三次验收写入 `false -> true -> false` 后恢复 `true`，四次请求均由 sub2api 访问日志确认；Core 环境、日志、数据库均无网关 URL 或管理 key，Connector 环境也无二者，本地三个私有文件均为 `0600`，enrollment 文件已清空。重置过程只删除 Core PG 与 sub2api Connector 私有卷，并保留重置前 Core dump；所有真实网关卷未触碰。另修复安装中断恢复：同账号同实例再次生成指南会原子替换未使用 enrollment，使旧 token 立即失效，不再等待 7 天过期。 | 已执行，待用户验收 |
| 5.3 | Connector 运行与监控配置 | 用户建议为 Connector 提供必要能力配置，例如心跳检查频率，并要求判断应由 Core 还是 Connector 管理以及具体开放哪些选项。 | 已按三层边界处理：Connector 的 task lease 轮询同时承担在线观测，不新增独立心跳；网关账号健康检查和自动处置是 Core 的每实例业务策略；网关请求超时、日志级别和本地连接诊断属于 Connector。普通用户不接触 lease、并发、协议版本、重试次数等内部参数。 | 已执行：Core 空闲轮询建议为 `5s`，Connector 执行任务后 `1s` 再取任务，失败按 `2/5/10/20/30s + jitter` 退避并遵循 `next_poll_after`；`last_seen_at` 无状态变化时最多每 `15s` 持久化，超过 `60s` 未见判离线。每 Instance 可配置监控开关、`30/60/300s` 检查间隔、连续失败 `1-5` 次、自动切换（默认关闭）、`5/15/30m` 冷却和漂移检测，并可立即检查。Connector 本地 UI 增加 `5-20s` 网关超时、`error/info/debug` 日志级别、Core/网关测试和脱敏诊断；Core 请求和网关请求使用独立 HTTP client，页面与 API 均不回显凭证、请求头、原始响应或原始错误。真实 sub2api 验收确认 migration `22`、策略保存与审计、2 个账号读取、Core/网关测试、15 秒写入节流、60 秒离线及恢复上线均正常，账号 `2,1` 保持 `schedulable=true`，真实网关数据卷未触碰。 | 已执行，待用户验收 |
| 5.4 | 控制台中文展示 | 用户发现中文模式下业务动作仍显示 `gateway.accounts.list`，市场页也存在英文展示，并要求全面排查同类问题。 | 判断合理。中文模式不能只翻译菜单和页面标题；业务枚举、状态、服务端错误、空状态和组件自带文案也属于用户界面。直接暴露内部 action key 会增加理解成本，也说明 i18n 覆盖边界不完整。 | 已执行：业务动作、能力名称/模式/说明、市场页列表与弹层、实例账号状态、健康检查错误、上游托管和系统设置均接入中文展示；统一 ProTable 分页行为并关闭英文页容量选择器；全页面巡检时额外修复号池健康页的 `No data` 空状态。已重建本地 Core，并通过构建、Lint、12 项相关测试、格式检查和浏览器实机验收；品牌、协议及必要技术标识继续保留英文。 | 已执行，待用户验收 |
